# Development environment configuration

# Proxy path (used in dev server)
VITE_PROXY_PATH=/api

# Backend API base URL (proxy target in dev server)
VITE_API_BASE_URL=http://localhost:3001/api/admin

# Use mock data (false = connect to real API)
VITE_USE_API_STUB=false
